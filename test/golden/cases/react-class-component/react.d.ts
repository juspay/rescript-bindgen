// Minimal React.Component stub (real @types/react is name-matched the same way). Kept in a
// separate module so these base classes aren't themselves exports of the entry → not bound.
export declare class Component<P = {}> { props: P }
export declare class PureComponent<P = {}> { props: P }
