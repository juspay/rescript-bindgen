// Namespace `Toolbar` of @base-ui-components/react — zero-cost aliases; use <Toolbar.Button />
module Button = ToolbarButton
module Group = ToolbarGroup
module Input = ToolbarInput
module Link = ToolbarLink
module Root = ToolbarRoot
module Separator = ToolbarSeparator
